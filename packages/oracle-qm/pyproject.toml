[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "oracle-qm"
version = "0.1.0"
description = "Shared QM data sections for ORACLE enriched XYZ containers"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
