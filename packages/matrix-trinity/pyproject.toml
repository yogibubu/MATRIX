[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "matrix-trinity"
version = "0.1.0"
description = "External energy/gradient geometry optimization workflows for MATRIX"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

