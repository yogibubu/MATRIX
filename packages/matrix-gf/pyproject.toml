[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "matrix-gf"
version = "0.1.0"
description = "GF/PED analysis for MATRIX"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

