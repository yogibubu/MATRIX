[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "oracle-rovib"
version = "0.1.0"
description = "Rotational, vibrational and rovibrational contracts for ORACLE"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
